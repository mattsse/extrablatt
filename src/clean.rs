use select::document::Document;
use select::predicate::Name;

pub struct DocumentCleaner;

impl DocumentCleaner {
    pub fn clean_doc(doc: &mut Document) {
        // find all nodes and remove their indices from the documentsdata
    }

    pub fn clean_body_classes(doc: &mut Document) {}
}
